name: Test 65 # Related to https://stackoverflow.com/questions/18995854/how-can-i-use-github-api-to-get-all-tags-or-releases-for-a-project

on:
  push:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $TOKEN" \
            --http1.1 https://api.github.com/repos/KnpLabs/The-office-app/commits/670fbcfdd2938dc2235ef71d7c622e8aa1fccd15/status
        env:
          TOKEN: ${{ secrets.ACCESS_TOKEN }}
